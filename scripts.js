// TODO: when we click already thn all fields should be hidden except username and password
// 1. Grab a reference the other button that says 'already'

const alreadyBtn = document.querySelector("#toggle-btn");
